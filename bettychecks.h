#ifndef SIMPLE_SHELL_H
#define SIMPLE_SHELL_H

#include <stdio.h>
/**
 * main - entry point for the program of simple_shell
 *
 * Return: Always 0 if successful
 * Description: The function as the entry point
 * of the program of simple_shell and it returns 0
 * the execution is successful.
 */
int main(void)
{
return (0);
}
#endif /*SIMPLE_SHELL_H*/
